# Feature Engineering Documentation

No unsupported synthetic features were forced into the model. Numeric variables use median imputation and scaling; categorical variables use most-frequent imputation and one-hot encoding. Business-impact variables are separated from predictive features to reduce leakage risk.
